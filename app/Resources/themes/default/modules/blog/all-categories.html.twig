{% extends "::base.html.twig" -%}

{% block SEO %}
    {{ generateSEOFromContent( customContentBlogViewAllCategories(true), "Browse All %module% Categories"|trans({"%module%" : "Blog"|trans}) ) }}
{% endblock -%}

{% block body -%}
    <main>
        <div class="container well well-light">
            <section id="allcategories">
                {{ customContentBlogViewAllCategories() }}
                <br>

                <h1 class="theme-title theme-title-centered">{{ "Explore All Deals Categories"|trans }}</h1>

                {% include "::blocks/all-categories.html.twig" -%}

                {{ customContentBlogViewAllCategoriesBottom() }}
            </section>
        </div>
    </main>
{% endblock -%}
