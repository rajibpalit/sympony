{% extends "::base.html.twig" -%}

{% block SEO %}
    {{ generateSEOFromContent( customContentEventViewAllLocations(true), "Browse All %module% Locations"|trans({"%module%" : "Event"|trans}) ) }}
{% endblock -%}

{% block body -%}
    <main>
        <div class="container well well-light">
            <section id="alllocations">
                {{ customContentEventViewAllLocations() }}
                <br>

                <h1 class="theme-title theme-title-centered">{{ "Explore All Events Locations"|trans }}</h1>

                {% include "::blocks/all-locations.html.twig" -%}

                {{ customContentEventViewAllLocationsBottom() }}
            </section>
        </div>
    </main>
{% endblock -%}
